import {DefaultTheme} from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#2A59FE',
    black: '#000000',
    white: '#FFFFFF',
    gray: '#64738C',
    red: '#F90000',
    overlay: 'rgba(0, 0, 0, 0.4)',
  },
};
